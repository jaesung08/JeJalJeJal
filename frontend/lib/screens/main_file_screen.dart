import 'package:flutter/material.dart';

class MainFileScreen extends StatelessWidget {
  const MainFileScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("파일 통역 페이지"),
      ),
    );
  }
}
