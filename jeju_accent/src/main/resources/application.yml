server:
  port: 8001
  servlet:
    context-path: "/api/"
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: JeJal
  datasource:
    hikari:
      driver-class-name: org.mariadb.jdbc.Driver
      username: S10P31A406
      password: dy12tnf34
    url: jdbc:mariadb://k10a406.p.ssafy.io:3306/S10P31A406?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: false
    properties:
      hibernate.format_sql: false
      dialect: org.hibernate.dialect.MySQL8InnoDBDialect



#logging
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: trace
    root: INFO
    org:
      springframework:
        web: INFO
        apache:
          tiles: DEBUG
        springframework:
          boot: DEBUG
  file:
    name: logs/json_file_upload.log
    pattern:
      file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"